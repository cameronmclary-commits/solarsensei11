// Heatmap gallery was removed from the page — kept as a no-op so the
// main.js import keeps working. Re-add rendering here if/when the
// uploaded heatmap visuals are reintroduced.
function renderHeatmaps() {}

export { renderHeatmaps };
